# Changesets

Use Changesets for package versioning and changelog generation.

Run `pnpm changeset` for any user-visible package change.
Foundation-only repository setup does not need a release changeset.
